import React from "react";
import { Text, View } from "react-native";
import PropTypes from "prop-types";
import { styles } from "./styles";

const ProjectSubmit = () => (
  <View>
    <Text>ProjectSubmit Scene</Text>
  </View>
);

// ProjectSubmit.propTypes = {};

export default ProjectSubmit;
