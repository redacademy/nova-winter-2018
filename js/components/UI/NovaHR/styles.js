import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  novaHROuterWrapper: {
    flex: 1,
    flexDirection: "row",
    justifyContent: "center",
    marginTop: 16,
    paddingBottom: 0
  },
  novaHRInner: {
    width: 160,
    height: 4
  }
});
